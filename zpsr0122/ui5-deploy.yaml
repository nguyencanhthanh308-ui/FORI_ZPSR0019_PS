# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: zpsr0122
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: s4
          url: http://s4prototype:443
          client: '320'
        app:
          name: ZPSR0122
          description: 組立可能日登録承認
          package: ZZZ01
          transport: DS4K917613
        exclude:
          - /test/
